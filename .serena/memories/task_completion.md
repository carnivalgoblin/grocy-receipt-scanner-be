# Task Completion Checklist

Before considering a task complete:
1. Ensure the code compiles: `./mvnw compile`
2. Run existing tests: `./mvnw test`
3. Add new tests if new functionality was added.
4. Verify that `mappings.json` is handled correctly if modified.
5. Check if `application.properties` needs updates (but don't commit secrets).
